window.addEventListener('load', function () {
  // document.addEventListener('DOMContentLoaded', function () {
  // When the content is loaded, add the 'loaded' class to the body
  document.body.classList.add('loaded');
});
